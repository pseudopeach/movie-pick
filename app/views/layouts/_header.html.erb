<header>
  <% if session[:user_id] %>
      <div>
        Logged in as <%= session[:name] %>
      </div>

      <div>
        <%= link_to "Log Out", controller: :users, action: :logout %>
      </div>
  <% end %>
</header>